#PR 175674 Get integer type values from media-cac file list

def mcacListContains(result, value):
    """
     Input: A String, And an Integer Value
       This takes input as the text which is to be searched for a particular
       value.  If the value is found in any of the row in the text, then it returns
       True else it returns False.
    """
    resultList = result.split('\r\n')
    resultList.pop()

    for res in resultList:
      val = int(res.split(":")[1].strip().strip("kbps").strip())

      # If the value is found in the text, then return True.
      if val == value:
        return True

    return False
