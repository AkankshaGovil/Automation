def readInputFile(DataNames,ipFileName):
       inputFile = open(ipFileName,'r')
       fileDataList = inputFile.readlines()
       inputFile.close()
       lineno = 0

       datasource = []
       for item in fileDataList[2:]:
       # Ticket 31356 - added code to skip the commented lines
       # in the input file
	   if item.startswith('#'):
               continue
           dict1 = {}
           itemList = item.split(':')
           for i in range(len(itemList)):
               dict1[DataNames[i]]=itemList[i].strip("\n")
           datasource.append(dict1)
       return datasource

